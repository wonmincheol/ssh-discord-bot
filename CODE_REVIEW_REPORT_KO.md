# SSH Discord Bot 코드 검토 보고서

- 검토일: 2026-09-05
- 검토 대상: `ssh-discord-bot` 저장소 전체
- 검토 방식: 소스·셸 스크립트·설정·문서 정적 검토, Python AST 구문 검사, 테스트 탐색, Git 작업 상태 확인
- 검토 범위 밖: 실제 Ubuntu/Palworld/Discord 연결을 사용한 통합 및 운영 시험

## 1. 결론

이 프로젝트는 Discord 슬래시 명령으로 Palworld 서버를 시작·중지·조회하고, 무접속 상태가 지속되면 별도 프로세스가 서버를 종료하는 소규모 개인 서버용 프로토타입이다. 파일 수가 적고 역할이 비교적 명확하게 나뉘어 있어 이해와 수정은 쉽다.

다만 현재 상태 그대로 운영하면 다음 두 문제가 가장 먼저 사고로 이어질 가능성이 높다.

1. `/start`, `/stop`, `/desktop_on` 명령에 사용자·역할·길드 제한이 없어 봇을 사용할 수 있는 사람이 서버와 데스크톱 전원을 제어할 수 있다.
2. 자동 종료 완료 알림이 `Interaction.send()`를 호출하는데, `discord.Interaction`의 정상적인 응답 경로가 아니므로 런타임 오류와 모니터 태스크 중단이 예상된다.

또한 동기식 외부 프로세스/API 호출이 Discord 이벤트 루프를 막고, 서버 시작 스크립트가 실제 시작 성공을 검증하지 않으며, 테스트가 전혀 없다. 따라서 현재 완성도는 **개인 실험용으로는 사용 가능하나 무인 운영용으로는 보강이 필요한 상태**로 판단한다.

## 2. 구조와 실행 흐름

```text
Discord 슬래시 명령
        │
        ├─ /status, /start, /stop ── sudo ── server-control/*.sh
        │                                      │
        │                                      └─ PalServer 프로세스
        │
        ├─ /players ── palworld_api.py ── Palworld REST API
        │
        └─ /start 성공 ── auto_shutdown.py 하위 프로세스
                                  │
                                  ├─ 주기적으로 플레이어 조회
                                  └─ 무접속 시간 초과 시 stop-palworld.sh
```

구성 파일별 책임은 다음과 같다.

| 파일 | 책임 |
|---|---|
| `bot.py` | Discord 명령, 서버 제어 프로세스 실행, 자동 종료 프로세스 추적 |
| `auto_shutdown.py` | 접속자 감시 및 시간 초과 시 서버 종료 |
| `palworld_api.py` | Palworld REST API의 플레이어 목록 조회 |
| `config.py` | 토큰, API 인증정보, 경로, 자동 종료 설정 |
| `server-control/*.sh` | PalServer 프로세스 상태 확인·시작·종료 |

## 3. 잘된 점

- `palworld_api.py`, `auto_shutdown.py`, 서버 제어 스크립트로 관심사를 나누어 전체 흐름을 빠르게 파악할 수 있다.
- REST 요청에 5초 타임아웃과 `raise_for_status()`가 있어 무한 대기와 HTTP 오류 무시를 일부 방지한다 (`palworld_api.py:12-21`).
- `subprocess` 호출에 문자열 셸 명령 대신 인자 배열을 사용해 셸 주입 위험을 줄였다 (`bot.py:57-60`, `69-73`, `91-95`, `146-150`).
- 시작 전 실행 여부를 확인하고, 자동 종료 프로세스도 중복 생성을 피하려는 방어가 있다 (`start-palworld.sh:3-7`, `bot.py:101-118`).
- 설정 가능한 자동 종료 시간과 확인 간격을 제공해 개인 서버 운영 목적에 맞추기 쉽다 (`config.py:28-34`).
- 현재 추적 파일에는 실제 Discord 토큰이나 Palworld 비밀번호가 아니라 자리표시자가 들어 있다.

## 4. 주요 발견 사항

### [긴급] F-01. 관리 명령에 접근 제어가 없다

- 근거: `/start`, `/stop`, `/desktop_on`을 포함한 모든 명령에 역할, 사용자, 길드 또는 Discord 권한 검사가 없다 (`bot.py:50-218`).
- 영향: 봇 명령에 접근 가능한 사용자가 게임 서버를 임의로 켜거나 끄고, 내부 데스크톱으로 Wake-on-LAN 패킷을 보낼 수 있다. 봇이 여러 길드에 추가되면 영향 범위도 넓어진다.
- 권고: 허용 길드 ID와 관리자 역할 ID를 환경변수로 관리하고 공통 `app_commands.check`로 강제한다. 파괴적 명령에는 `default_permissions(administrator=True)`도 적용하되, 이것만 믿지 말고 서버 측 허용 목록을 함께 사용한다. `/ping`, `/status`, `/players`와 `/start`, `/stop`, `/desktop_on`의 권한 등급을 분리한다.

### [높음] F-02. 자동 종료 알림 호출이 잘못되어 모니터 전체가 중단될 수 있다

- 근거: 자동 종료 후 저장해 둔 `discord.Interaction`에 `session["interaction"].send(...)`를 호출한다 (`bot.py:40-46`). 일반적인 상호작용 후속 메시지는 `interaction.followup.send(...)`를 사용해야 한다.
- 영향: 자동 종료 자체는 자식 프로세스에서 완료될 수 있지만 Discord 알림은 실패하고, 예외를 잡지 않아 `monitor_auto_shutdown()` 태스크 자체가 종료된다. 그 이후 다른 세션 정리와 알림도 처리되지 않는다.
- 권고: 오래 유지되는 `Interaction` 객체 대신 알림 대상 채널 ID를 저장하고 `bot.get_channel()`/`fetch_channel()` 후 전송한다. 최소한 후속 메시지를 쓸 경우 `interaction.followup.send()`를 사용하고, 세션별 예외 처리와 `finally` 정리를 추가한다.

### [높음] F-03. 서버 시작 성공을 확인하지 않고 `STARTED`를 반환한다

- 근거: `start-palworld.sh`는 PalServer 스크립트를 백그라운드 실행한 직후 조건 없이 `STARTED`를 출력한다 (`start-palworld.sh:9-11`). `bot.py`도 종료 코드보다 이 문자열만 신뢰한다 (`bot.py:91-120`).
- 영향: 실행 파일 부재, 권한 오류, 즉시 크래시가 발생해도 Discord에는 시작 성공으로 표시되고 자동 종료 모니터까지 실행될 수 있다.
- 권고: 실행 후 제한 시간 동안 PID 또는 서비스 상태를 재확인하고 성공할 때만 0과 `STARTED`를 반환한다. 실패 시 stderr와 비영(非零) 종료 코드를 반환한다. 가능하면 직접 프로세스 탐색보다 `systemd` 서비스를 사용한다.

### [높음] F-04. Discord 이벤트 루프에서 블로킹 작업을 실행한다

- 근거: 비동기 명령 함수 안에서 `subprocess.run()`과 동기 `requests.get()`을 직접 호출한다 (`bot.py:57-61`, `69-73`, `91-95`, `146-150`, `169-194`; `palworld_api.py:12-19`). 서버 제어 subprocess에는 타임아웃도 없다.
- 영향: sudo, 셸 스크립트 또는 REST API 응답을 기다리는 동안 다른 명령 응답과 Discord heartbeat가 지연된다. 3초 내 초기 응답이 필요한 상호작용도 실패할 수 있다.
- 권고: 명령 진입 즉시 `interaction.response.defer()`하고, subprocess는 `asyncio.create_subprocess_exec()`와 타임아웃으로 실행한다. API는 비동기 HTTP 클라이언트를 사용하거나 최소한 `asyncio.to_thread()`로 격리한다.

### [높음] F-05. 인증정보를 소스 파일에서 직접 관리하도록 설계되어 있다

- 근거: 토큰과 API 비밀번호 설정이 `config.py` 상수에 위치하고 README도 이 파일을 직접 편집하도록 안내한다 (`config.py:5-13`, `README.md:56-94`). `.gitignore`와 예제 설정 파일이 없다.
- 영향: 실제 값을 입력한 `config.py`를 커밋하거나 공유할 가능성이 높다. Discord 토큰 유출 시 봇 계정 탈취, Palworld 관리자 비밀번호 유출 시 서버 관리 API 오용으로 이어질 수 있다.
- 권고: 비밀값은 환경변수 또는 권한이 제한된 `.env`에서 읽고, `.env`를 `.gitignore`에 넣는다. 저장소에는 `.env.example`만 둔다. 시작 시 자리표시자나 누락된 값이면 명확히 실패하도록 설정 검증을 추가한다.

### [중간] F-06. 봇 재접속 때 자동 종료 감시 태스크가 중복 생성된다

- 근거: `on_ready()`가 호출될 때마다 `bot.loop.create_task(monitor_auto_shutdown())`를 실행하며 기존 태스크 여부를 확인하지 않는다 (`bot.py:227-233`). Discord 재접속 시 `on_ready`는 다시 호출될 수 있다.
- 영향: 동일한 `server_sessions`를 여러 루프가 동시에 순회하고 삭제해 `KeyError`, 중복 알림 또는 모니터 태스크 종료가 발생할 수 있다. 명령 동기화도 불필요하게 반복된다.
- 권고: `setup_hook()`에서 명령 동기화와 태스크 시작을 한 번만 수행하거나 `tasks.loop`와 `before_loop`를 사용한다. 태스크 핸들을 보관하고 중복 실행을 막는다.

### [중간] F-07. 자동 종료 프로세스의 생명주기가 서버 상태와 일치하지 않는다

- 근거: 자동 감시는 `/start` 결과가 정확히 `STARTED`일 때만 생성된다 (`bot.py:99-118`). `ALREADY_RUNNING`이면 시작되지 않으며, 봇 재시작 후 이미 실행 중인 PalServer도 감시하지 않는다. 반대로 `AUTO_SHUTDOWN_ENABLED=False`여도 자식 프로세스는 계속 실행된다 (`auto_shutdown.py:18-68`).
- 영향: 가장 필요한 무인 상황에서 자동 종료가 동작하지 않거나, 불필요한 영구 프로세스가 남을 수 있다.
- 권고: 봇 시작 시 실제 서버 상태를 기준으로 감시를 복구하고, 설정이 꺼져 있으면 감시를 시작하지 않는다. 장기적으로 감시 기능을 봇 내부의 단일 `tasks.loop` 또는 별도 systemd 서비스로 통합한다.

### [중간] F-08. 자동 종료 실패 세션이 정리되지 않는다

- 근거: 자식 프로세스는 종료 실패 시 `AUTO_SHUTDOWN_FAILED`를 출력하고 종료하지만 (`auto_shutdown.py:53-62`), 부모는 `AUTO_SHUTDOWN`이 포함된 경우에만 세션을 삭제한다 (`bot.py:36-46`).
- 영향: 실패한 세션이 `server_sessions`에 남고 3초마다 종료된 프로세스를 다시 검사한다. 사용자는 실패 사실을 통지받지 못한다.
- 권고: 성공·실패·예상치 못한 종료를 모두 상태로 처리하고 `finally`에서 세션을 제거한다. 실패 알림과 제한된 재시도 정책도 추가한다.

### [중간] F-09. 수동 종료와 모니터 프로세스 종료 처리에 경쟁 조건이 있다

- 근거: `/stop`은 감시 프로세스에 `terminate()`만 보내고 종료를 기다리지 않은 채 딕셔너리에서 삭제한다 (`bot.py:135-150`). 동시에 3초 주기 모니터가 같은 항목을 읽거나 삭제할 수 있다 (`bot.py:30-48`).
- 영향: 자식 프로세스가 잠시 남거나, 중복 삭제 예외로 모니터가 중단될 수 있다. 종료 직전 자식이 stop 스크립트를 실행하면 수동 종료와 겹칠 수도 있다.
- 권고: 세션별 lock을 사용하고 `terminate → wait(timeout) → kill` 순서로 확실히 회수한다. 딕셔너리 삭제는 `pop(key, None)`과 `finally`를 사용한다.

### [중간] F-10. 프로세스 이름 검색 기반 제어와 종료 검증이 약하다

- 근거: 세 스크립트 모두 `pgrep/pkill -f "PalServer-Linux-Shipping"`에 의존한다. 종료 스크립트는 `pkill` 성공만으로 `STOPPED`를 출력하고 실제 종료를 재확인하지 않는다 (`server-control/stop-palworld.sh:3-14`).
- 영향: 명령행이 우연히 일치하는 다른 프로세스를 오인하거나, 신호 전송 후 프로세스가 남아 있어도 성공으로 표시할 수 있다. 강제 프로세스 종료 방식은 애플리케이션의 정상 종료 절차를 보장하지 않는다.
- 권고: PalServer를 systemd 단위로 관리하여 `systemctl is-active/start/stop`과 서비스 종료 타임아웃을 사용한다. 현재 구조를 유지한다면 정확한 PID 파일, 실행 사용자 확인, 종료 후 재검증을 추가한다.

### [낮음] F-11. 사용자·오류 문자열을 그대로 Discord 메시지에 삽입한다

- 근거: 플레이어 이름을 이스케이프 없이 연결하고 (`bot.py:182-188`), 예외 및 stderr를 그대로 코드 블록에 노출한다 (`bot.py:83-84`, `125-128`, `163-166`, `190-194`).
- 영향: 특수한 플레이어 이름으로 멘션/마크다운이 삽입되거나, 내부 경로·API 오류 정보가 채널에 공개될 수 있다. 긴 출력은 Discord 메시지 제한을 넘을 수 있다.
- 권고: `discord.utils.escape_markdown()`과 `AllowedMentions.none()`을 적용하고 길이를 제한한다. 사용자에게는 짧은 오류 ID만 보여 주고 상세 오류는 운영 로그에 남긴다.

### [낮음] F-12. 설정·문서·구현이 서로 어긋난다

- 근거:
  - README는 `!start` 같은 접두사 명령으로 설명하지만 구현은 `/start` 슬래시 명령이다 (`README.md:13-23`, `bot.py:50-218`).
  - README의 영문 설정 경로는 `/data/discord-bot`이지만 실제 설정은 `/data/ssh-discord-bot`이다 (`README.md:78`, `config.py:19`).
  - README의 향후 계획에 슬래시 명령이 남아 있지만 이미 구현되어 있다 (`README.md:224-233`).
  - `/desktop_on` 명령과 하드코딩된 브로드캐스트 IP/MAC은 문서와 설정 파일에 없다 (`bot.py:197-218`).
  - `AUTO_SHUTDOWN_TIME`이 1시간이 아니어도 알림은 항상 “an hour”라고 표시한다 (`bot.py:42-44`, `config.py:31`).
- 영향: 설치 실패, 잘못된 사용법, 네트워크별 수정 누락 가능성이 커진다.
- 권고: README 예제를 실제 슬래시 명령과 경로에 맞추고, WOL 값을 설정으로 이동한다. 시간 문구는 설정값에서 생성한다. 영문/국문 중복 내용은 동일 목차로 유지하거나 별도 문서로 분리한다.

## 5. 검증 결과

| 항목 | 결과 | 비고 |
|---|---|---|
| Python AST 구문 검사 | 통과 | `auto_shutdown.py`, `bot.py`, `config.py`, `palworld_api.py` |
| 테스트 탐색 | 0개 | `unittest discover` 결과 `NO TESTS RAN` |
| Git diff 형식 검사 | 통과 | 기존 코드의 whitespace 오류 없음 |
| 작업 트리 | 깨끗함 | 보고서 작성 전 사용자 변경 사항 없음 |
| Bash 구문 검사 | 미실행 | 현재 Windows 환경에서 WSL Bash 인스턴스 접근 불가 |
| 실제 통합 실행 | 미실행 | Discord 토큰, Palworld 서버, Linux sudo 환경이 필요 |

Python 패키지는 현재 검토 환경에 설치되어 있지 않아 `discord.py` 객체 수준의 런타임 시험은 수행하지 않았다. `requirements.txt`는 최소 버전만 지정하므로 미래 버전 변경에 따른 회귀를 재현하기 어렵다.

## 6. 테스트 공백

현재 자동화 테스트와 CI 설정이 없다. 최소한 다음 시나리오가 필요하다.

1. 서버 제어 결과 `STARTED`, `ALREADY_RUNNING`, 실패/타임아웃 매핑
2. 서버 중지 결과 `STOPPED`, `ALREADY_STOPPED`, 실패/타임아웃 매핑
3. 플레이어 0명/1명/다수/API 오류/잘못된 JSON 처리
4. 무접속 타이머 시작, 플레이어 재접속 시 초기화, 시간 초과 종료
5. 자동 종료 성공·실패·자식 프로세스 비정상 종료 시 세션 정리
6. 봇 재접속 후 모니터 태스크가 하나만 존재하는지 확인
7. 허용/비허용 길드·역할·사용자별 관리 명령 접근 제어
8. 셸 스크립트의 시작 실패, 종료 후 잔존 프로세스, 오인 탐지 방지

테스트 가능성을 높이려면 현재 모듈 최상위의 `bot.run()`과 `auto_shutdown.py`의 무한 루프를 `main()`으로 감싸고, subprocess/API/시간/Discord 전송을 주입 가능한 함수 또는 클래스로 분리하는 것이 좋다.

## 7. 권장 개선 순서

### 즉시 조치

1. 관리 명령에 길드·역할 허용 목록을 적용한다.
2. Discord 토큰과 API 비밀번호를 환경변수로 이동하고 유출 여부를 확인한 뒤, 실제 값이 한 번이라도 커밋됐다면 즉시 교체한다.
3. 자동 종료 알림 전송과 실패 세션 정리를 수정한다.
4. 시작·종료 스크립트가 실제 상태를 확인하고 올바른 종료 코드를 반환하게 한다.

### 단기 조치

1. 블로킹 subprocess/API 호출을 비동기로 전환하고 모든 외부 호출에 타임아웃을 둔다.
2. 자동 종료를 단일 관리 태스크로 통합하고 봇 재시작 시 상태를 복구한다.
3. 핵심 상태 전이 단위 테스트와 GitHub Actions를 추가한다.
4. README, 경로, 명령 표기, WOL 설정을 현재 구현과 일치시킨다.

### 중기 조치

1. PalServer 프로세스 관리를 systemd로 위임한다.
2. 구조화 로그, 오류 ID, 시작·종료·명령 실행 감사 로그를 추가한다.
3. 의존성 버전을 잠그고 정기적인 보안 업데이트 절차를 둔다.

## 8. 최종 평가

| 영역 | 평가 |
|---|---|
| 구조 단순성 | 양호 |
| 핵심 기능 구현 | 보통 |
| 보안 | 미흡 — 관리 명령 접근 제어와 비밀 관리가 최우선 |
| 안정성 | 미흡 — 자동 종료 알림, 중복 태스크, 블로킹 호출 개선 필요 |
| 테스트/운영성 | 미흡 — 테스트·CI·서비스 관리·로그 부재 |
| 문서 | 보통 — 내용은 풍부하지만 구현과 불일치 |

핵심 아이디어와 기본 구조는 분명하다. 권한 통제, 자동 종료 생명주기, 비동기 처리, 실제 프로세스 상태 검증을 먼저 보완하면 개인 서버용 도구에서 신뢰할 수 있는 운영 도구로 발전시킬 수 있다.
